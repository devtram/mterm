/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import Resume from './src/resume'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Resume);
